services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: rad-gallery-frontend
    restart: unless-stopped

    env_file:
      - .env.production

    expose:
      - "5173"

    networks:
      - web

networks:
  web:
    external: true